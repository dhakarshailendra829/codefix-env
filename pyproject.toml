[project]
name = "codefix-env"
version = "1.0.0"
description = "Python code debugging environment"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn", "pydantic", "openenv-core"]
[project.scripts]
server = "server.app:main"
[tool.openenv]
environment_class = "CodeFixEnvironment"
